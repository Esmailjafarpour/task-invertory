function Footer(params) {
  return (
    <div
      style={{
        background: "#fff",
        height: "100px",
        display: "flex",
        justifyContent: "center",
        alignItems : "center"
      }}
    >
      <h1>Footer</h1>
    </div>
  );
}

export default Footer;
