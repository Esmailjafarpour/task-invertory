function Header(params) {
  return (
    <div
      style={{
        background: "#fff",
        height: "100px",
        display: "flex",
        justifyContent: "center",
        alignItems : "center"
      }}
    >
      <h1>Header</h1>
    </div>
  );
}

export default Header;
